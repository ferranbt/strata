[package]
name = "strata-dockertest"
version = "0.1.0"
edition = "2024"

[dependencies]
testcontainers = "0.23"
anyhow = "1"
tokio = { version = "1", features = ["time"] }

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "time"] }
